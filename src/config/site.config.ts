/**
 * Active prospect config — switched by scripts/build-previews.mjs during multi-builds.
 * Default on main: Harbour demo sample.
 */
export type { ServiceItem, SiteConfig } from "./types";
export { default } from "../prospects/harbour";
